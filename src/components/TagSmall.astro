---
interface Props {
    tags: string[];
    limit?: number;
}

const { tags, limit } = Astro.props;
const visibleTags = typeof limit === "number" ? tags.slice(0, limit) : tags;
---

{
    visibleTags.length > 0 && (
        <ul class="flex flex-wrap gap-1.5" aria-label="文章标签">
            {visibleTags.map((tag) => (
                <li>
                    <span class="tag-pill">
                        #{tag}
                    </span>
                </li>
            ))}
        </ul>
    )
}
